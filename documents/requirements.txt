# ============================================
# 知识库问答系统 - 依赖库清单
# 生成时间: 2026年5月21日
# Python版本要求: >=3.8
# ============================================

# Web框架
streamlit>=1.31.0

# LangChain核心（建议使用近期稳定的次新版本，避免最新版的breaking change）
langchain==0.2.0
langchain-community==0.2.0
langchain-core==0.2.0
langchain-text-splitters==0.2.0
langchain-chroma==0.1.1

# 向量数据库（解除严格锁定，让它自动适配 LangChain 的依赖）
chromadb>=0.5.0

# 阿里云API服务
dashscope>=1.14.0

# 文档加载器（解除小版本锁定，避免编译失败）
PyPDF2
python-docx

# 文本处理
tiktoken

# 数据科学库
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
plotly>=5.18.0

# 混合检索和重排序
rank-bm25>=0.2.2  # BM25关键词检索
jieba>=0.42.1     # 中文分词
sentence-transformers>=2.2.2  # CrossEncoder重排序模型（可选）